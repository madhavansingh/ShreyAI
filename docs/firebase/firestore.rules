rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo mode — open read/write for all (no user auth)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
